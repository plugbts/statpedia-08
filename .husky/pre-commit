#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Run staged file linters, formatters, and type-check staged TS files only
npx --no-install lint-staged

# Tip: full project checks run in CI. If you want to enforce locally before push,
# create a pre-push hook that runs `npm run check`.
